package randori.backbone {

	import randori.webkit.dom.Element;
	import randori.jquery.JQuery;

	[JavaScript(export="false",name="Backbone.history")]
	public class History {

		/**
		 * <p id="History-start">
		 *   <b class="header">start</b>
		 *   <code>Backbone.history.start([options])</code>
		 *   When all of your
		 *   <a href="http://backbonejs.org/#Router">Routers</a>
		 *   have been created,
		 *       and all of the routes are set up properly, call
		 *   <tt>Backbone.history.start()</tt>
		 *   to begin monitoring
		 *   <tt>hashchange</tt>
		 *   events, and dispatching routes.
		 * </p>
		 */
		public static function start(options:Object=null):void {
		}
	}
}